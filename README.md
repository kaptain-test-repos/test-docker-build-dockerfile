# Test Docker Dockerfile

E2E test repo for `docker-build-dockerfile` workflow in buildon-github-actions.

Tests mustache token style, UPPER_SNAKE name style, squash-all, nested user config tokens (Category/SubVar), and multi-platform builds (linux/amd64,linux/arm64).
